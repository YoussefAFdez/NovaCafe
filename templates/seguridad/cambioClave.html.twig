{% extends 'base.html.twig' %}

{% block title %}Modificar Clave - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Modificar Clave</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Guardar datos</button>
    <a class="btn btn-primary" href="{{ path('principal') }}">Volver a inicio</a>

{% endblock %}
