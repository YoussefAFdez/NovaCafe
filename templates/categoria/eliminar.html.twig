{% extends 'base.html.twig' %}

{% block title %}
    Eliminar Categoría - {{ parent() }}
{% endblock %}

{% block body %}
    <h1>Eliminación de Categoría</h1>
    <h2>Estás a punto de eliminar la categoría "{{ categoria }}". ¿Estás seguro de ello? Esta acción no se puede deshacer.</h2>
    <form method="post">
        <button type="submit" name="confirmar" value="si" class="btn btn-danger">Borrar permanentemente</button>
        <a href="{{ path('categoria_listar') }}" class="btn btn-primary">Volver al listado</a>
    </form>
{% endblock %}